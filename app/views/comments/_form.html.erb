<%= form_for [comment, Comment.new] do |f| %>
	<div class="form-group">
		<%= f.text_area :content, class: "form-control", rows: "2", placeholder: "Comment..." %>
		<%= f.hidden_field :whichpost, value: @post.id %>
	</div>
	<%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>